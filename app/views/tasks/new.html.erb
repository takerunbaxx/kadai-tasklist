<h1>タスクの作成</h1>

<%=form_with(model: @task, local: true) do |f|%>
<%= render 'layouts/error', model: @task %>
<%=f.label :content, "タスク"%>
<%=f.text_field :content%>

<%= render 'layouts/form', model: @task %>


<%end%>

<%=link_to "タスク一覧に戻る", tasks_path %>